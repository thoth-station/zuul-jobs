---
- hosts: all

  tasks:
    - name: Trigger OpenShift Build
      uri:
        method: POST
        url: "{{ webhook_url }}/{{ sesheta_webhooks_generic_webhook_secret.key }}/generic"
        body_format: json
        content: ""
        validate_certs: no
        follow_redirects: all
        headers:
          Content-Type: "application/json"
